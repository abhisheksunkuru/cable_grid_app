<div data-user-id="<%= @user ? @user.id : params[:user_id] %>" class="tiles-list">

<table>
  <%for row in 1..20 do %>
   <tr>
    <% for col in 1..20 do%>
      <td><%= generate_tile(row,col)%></td>
    <%end%>
   </tr>
  <%end%>
</table>
</div>